/**
 * <h> interface
 */
export interface IHeading {
  level: number;
  text: string;
  id: string;
}

/**
 * notion tag interface
 */
export interface INotionTag {
  color: string;
  id: string;
  name: string;
}
