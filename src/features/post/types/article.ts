/**
 * <h> interface
 */
export interface Heading {
  level: number;
  text: string;
  id: string;
}

/**
 * post tag interface
 */
export interface Tag {
  color: string;
  id: string;
  name: string;
}
