# 버전 관리 설정 (#107). .github/actions/release-config 가 읽는다.
# 값을 바꾸면 GitHub 룰셋의 대상 브랜치도 함께 바꾼다.

# 머지 1건이 릴리즈 1건인 브랜치. 여러 개면 공백으로 구분한다 (ex. "v3 v4")
RELEASE_BRANCHES="v3"

# 버전을 읽고 쓰는 파일. version 필드를 쓴다
VERSION_FILE="package.json"
